/*global define, console */
define(function (require) {
    'use strict';
    var jquery = require("jquery");

    return {
        init: function () {
            console.log("test" + jquery);
        }
    };
});
